using Benchwarp.Data;
using ItemChanger.Silksong.RawData;

namespace ItemChangerTesting.LocationTests;

/*
internal class BenjinCrullShinyTest : Test
{
    public override TestMetadata GetMetadata() => new()
    {
        Folder = TestFolder.LocationTests,
        MenuName = "Modify Tacks and Steel Spines",
        MenuDescription = "Tests modifying Tacks and Steel_Spines shinies in-place to give Surgeon's_Key and Flea. (requires act 3 setting)",
        Revision = 2026041000,
    };

    public override void Setup(TestArgs args)
    {
        StartNear(SceneNames.Dust_Shack, PrimitiveGateNames.left1);
        Profile.AddPlacement(Finder.GetLocation(LocationNames.Tacks)!.Wrap()
            .Add(Finder.GetItem(ItemNames.Surgeon_s_Key)!)
            .Add(Finder.GetItem(ItemNames.Flea)!));
        Profile.AddPlacement(Finder.GetLocation(LocationNames.Steel_Spines)!.Wrap()
            .Add(Finder.GetItem(ItemNames.Surgeon_s_Key)!)
            .Add(Finder.GetItem(ItemNames.Flea)!));

    }
}
*/
