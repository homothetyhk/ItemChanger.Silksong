using Benchwarp.Data;
using ItemChanger.Silksong.RawData;

namespace ItemChangerTesting.LocationTests;
/*
internal class SurgeonsKeyTest : Test
{
    public override TestMetadata GetMetadata() => new()
    {
        Folder = TestFolder.LocationTests,
        MenuName = "Modified Surgeon_s_Key",
        MenuDescription = "Tests modifying Surgeon_s_Key in-place to give Everbloom and Flea.",
        Revision = 2026040700,
    };

    public override void Setup(TestArgs args)
    {
        StartNear(SceneNames.Ward_07, PrimitiveGateNames.bot1);
        Profile.AddPlacement(Finder.GetLocation(LocationNames.Surgeon_s_Key)!.Wrap()
            .Add(Finder.GetItem(ItemNames.Everbloom)!)
            .Add(Finder.GetItem(ItemNames.Flea)!));
    }
}
*/