using Benchwarp.Data;
using ItemChanger.Silksong.RawData;

namespace ItemChangerTesting.LocationTests;
/*
internal class KeyOfApostateTest : Test
{
    public override TestMetadata GetMetadata() => new()
    {
        Folder = TestFolder.LocationTests,
        MenuName = "Modified Key_of_Apostate",
        MenuDescription = "Tests modifying Key_of_Apostate in-place to give Surgeon's_Key and Flea.",
        Revision = 2026040500,
    };

    public override void Setup(TestArgs args)
    {
        StartNear(SceneNames.Aqueduct_04, PrimitiveGateNames.right1);
        Profile.AddPlacement(Finder.GetLocation(LocationNames.Key_of_Apostate)!.Wrap()
            .Add(Finder.GetItem(ItemNames.Surgeon_s_Key)!)
            .Add(Finder.GetItem(ItemNames.Flea)!));
    }
}
*/